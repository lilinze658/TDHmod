add_namespace = WORLD
news_event = {		#亚人暴动
	id = WORLD.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event global.1"}
	title = WORLD.1.t
	desc = WORLD.1.d
#	picture = 

	is_triggered_only = yes

	option = {
		name = WORLD.1.a
		trigger = {
			original_tag = RED
		}
	if = {
		limit = {
			is_ai = no
		}
		set_temp_variable = { TDH_temp_super_event = token:SE_LOVINIA_CIVIL_WAR }
		TDH_fire_super_event = yes
	}
	
	}
	option = {
		name = WORLD.1.b
		trigger = {
			NOT = { original_tag = RED }
		}
	if = {
		limit = {
			is_ai = no
		}
		set_temp_variable = { TDH_temp_super_event = token:SE_LOVINIA_CIVIL_WAR }
		TDH_fire_super_event = yes
	}
	
	}
}